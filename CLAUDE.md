@AGENTS.md

# UsefulDesk — Product Context (India-First Gym CRM)

> Shared context for every session. UsefulDesk is being turned into an **India-first CRM for gyms, boutique studios, and (eventually) franchises**. Market analysis lives in `PRDs/india_gym_crm_pain_points.md`.

> **Keep this file current (rule, non-negotiable).** Every time you land a **major architecture change or a new feature**, update this CLAUDE.md in the *same* change — treat the doc edit as part of "done", not a follow-up. Touch the sections it affects (Architecture, UI reusability, Current status / "Built since", Roadmap To-Do, data model). Keep entries terse and factual: what shipped, where the code lives, any reuse/gotcha a future session must know. Prune stale lines rather than stacking contradictions. Small bug fixes / refactors don't need an entry; anything that changes how the codebase works or adds user-facing capability does.

## The one thing to understand first

This repo (`wacrm` v0.7.0) **looks** like a generic WhatsApp CRM but is **not primitive** — it's a mature multi-tenant WhatsApp CRM. It already ships best-in-class versions of the exact layer the market wants (WhatsApp send + templates, broadcasts, automations, shared inbox, RBAC, public API). What is genuinely missing is the **gym domain** (members, plans, memberships, renewals, payments, attendance).

So the strategy is **not** "build a CRM." It is: **keep the WhatsApp/comms foundation, add a gym domain layer on top, wire the two together.** The renewal + WhatsApp workflow that wins this market is ~60% already built.

## Market insights (from the PRD)

Indian gyms don't lack CRM features — they lack a *simple, reliable, WhatsApp-first, UPI-aware* layer for the messy exceptions (renewals, collections, follow-ups, lapsed members). They fall back to WhatsApp + Excel + phone + paper. Biggest pains, most urgent first:

1. **Payments / renewals / reconciliation** — manual chasing via WhatsApp/UPI/cash/Sheets. Revenue leakage. Most monetizable.
2. **WhatsApp is the real CRM** — should be the core channel, not an add-on.
3. **Reports aren't actionable** — owners want *action lists* ("who to call today"), not dashboards.
4. Reliability/sync failures · heavy onboarding/too many clicks · weak staff mobile workflows · weak family/multi-location plans.

**The wedge:** *Know who's expiring → remind on WhatsApp → collect on UPI → assign follow-up → track the conversation.* Everything else is secondary until this loop works.

## Product principles (non-negotiable)

Phone-first · WhatsApp-native (not add-on) · renewal-first · **action lists over dashboards** · simple-for-boutiques / controlled-for-franchises · offline-tolerant · payments + reminders in one flow · every exception has an owner + status + next action · don't force a member app early · owner feels in control in 30 seconds.

**Every feature must answer:** does it help the owner save time, recover lost leads, collect renewals, or retain members? If not — defer.

## Architecture (how this codebase works)

- **Stack:** Next.js 16 (App Router; middleware is renamed `proxy` — see `src/proxy.ts`), React 19, TypeScript, Supabase (Postgres + Auth + Storage + RLS), Tailwind v4, Base UI / shadcn primitives (`src/components/ui/`), **Motion (`motion/react`) for animation**. No zod (validation hand-rolled). Vitest, colocated `*.test.ts` in `src/lib`.
- **No `"use server"` actions.** Backend = `src/app/api/**/route.ts` handlers OR direct Supabase browser-client calls from client components, secured by Postgres RLS.
- **Tenancy:** tenant = `accounts` row; users belong to one via `profiles.account_id`. RBAC `owner > admin > agent > viewer` (`account_role_enum`) enforced by RLS helper `is_account_member(account_id, min_role)`; mirrored in `src/lib/auth/roles.ts` + `useAuth()` (`canEditSettings`, `canSendMessages`). **Copy the RLS pattern from `supabase/migrations/017_account_sharing.sql`.**
- **Migrations:** sequential + idempotent in `supabase/migrations/` (enum in `DO $$ IF NOT EXISTS` block, `CREATE TABLE/INDEX IF NOT EXISTS`, drop-then-create policies, reuse `update_updated_at_column()`). Latest is `057` (applied live via MCP; note `supabase db push` fails on a migration-history mismatch with the MCP-applied timestamped versions — always apply via MCP `apply_migration`). The live Supabase project is `UsefulDesk` (`fwqthstqrkrwtaehefks`) — new migrations are applied to it via the Supabase MCP `apply_migration` (or `supabase db push`); after applying, verify with a `pg_policies`/schema query.
- **Localization (migration `055`, non-negotiable pattern):** every gym (account) carries its own regional config on `accounts` — `country_code, locale, timezone, date_order ('DMY'|'MDY'|'YMD'), time_format ('12h'|'24h'), week_start (0|1|6), phone_country_code, measurement_system` + the pre-existing `default_currency`. **Geography lives ONLY in `src/lib/locale/config.ts`** (`COUNTRY_PRESETS` — one row per country; adding a country = adding a preset). `resolveAccountLocale(row)` narrows the account row (field-by-field India fallback) into an `AccountLocale`; `buildFormatters(cfg)` (`src/lib/locale/format.ts`) is the ONE formatting surface — `fmt.date/dateShort/time/dateTime/number/money/moneyShort/today()`. Client: `useLocale()` (`src/hooks/use-locale.ts`) returns the AuthProvider's memoized `{ locale, fmt }` (AccountSummary in `use-auth.tsx` selects the columns; `refreshProfile()` re-resolves after a settings save). Server/cron: `resolveAccountLocale(accountRow)` + `buildFormatters`. **Never** hand-roll `toLocaleDateString`/`toLocaleString`/`Intl.NumberFormat`/`format(date-fns)` for gym-domain output, never write `'en-IN'`/`'Asia/Kolkata'`/`if (country === …)` at a call-site. Timezone helpers for instants: `todayInTz`, `hourInTz`, `dayStartInTz` (day-start queries), `dateAtNoonInTz` (stamping picked days into timestamptz, e.g. `payments.paid_at`), `timeInTzToUtc`/`hhmmInTz` (reminder slots). `istToday()` survives only as the India-default fallback for pure-lib `today` params — components always pass `fmt.today()`. Signup has a country picker → preset → `auth.signUp` metadata → `handle_new_user` (055, regex-guarded) writes the columns; Settings → Localization (`localization-settings.tsx`) edits everything (country change re-applies the preset; live preview). UPI is gated by `upiAvailableFor(currency)` (INR-only rail — a currency condition, not a geo conditional). CSV-import ambiguous dates default to `importDateOrder(locale)`. Scope = regional formatting, NOT string translation.
- **Authored-content ownership (rule):** notes (and any future authored content — comments, activity remarks) belong to their author. Only the author may edit; only the author **or admin/owner** may delete (moderation). Enforced in BOTH layers: RLS (`user_id = auth.uid() AND is_account_member(account_id,'agent')`, plus `OR is_account_member(account_id,'admin')` on delete — see `046_contact_notes_ownership.sql`) and UI (gate the affordance via a `roles.ts` predicate, e.g. `canDeleteAnyNote`). Never a UI-only gate.
- **Role capabilities:** every "who can do X" check is a named predicate in `src/lib/auth/roles.ts` (+ test in `roles.test.ts`), mirrored by the RLS policy. Adding a capability = one predicate + one policy — no inline role comparisons at call-sites.
- **Supabase RLS-blocked writes are silent:** `.delete()`/`.update()` blocked by RLS return **no error, zero rows**. For destructive ops, chain `.select('id')` and treat an empty result as failure — otherwise the UI toasts success while the row survives (see `deleteNote` in `contact-detail-view.tsx`).
- **Reuse, don't rebuild:** WhatsApp send = `sendMessageToConversation` (`src/lib/whatsapp/send-message.ts`); dashboard send route `POST /api/whatsapp/send` accepts `contact_id` + `message_type:'template'` and **find-or-creates the conversation** (reaches members with no thread). Contact dedupe = `findExistingContact` / `isUniqueViolation` (`src/lib/contacts/dedupe.ts`). Media upload = `uploadAccountMedia('chat-media', file)` (`src/lib/storage/upload-media.ts`). All regional formatting (currency, dates, times, numbers, "today") = the account locale layer — `useLocale().fmt` client-side, `buildFormatters(resolveAccountLocale(row))` server-side (see the Localization bullet); `formatCurrency(value, currency, locale?)` (`src/lib/currency.ts`) underlies `fmt.money`.
- **Lint gotcha:** repo enforces `react-hooks/set-state-in-effect`. Never call a setState-wrapping function directly in `useEffect`. Load data with an inline `(async () => { … })()` IIFE + a `cancelled` guard; expose manual refetch as a nonce bump. See `src/components/members/*`.

## UI component reusability (non-negotiable)

- **One header per page — title, actions, and sub-nav tabs all live in the shared app bar** (`src/components/layout/header.tsx`). A page must **not** own a second title/subtitle row or a standalone tab strip. The header is a two-row `flex-col` with the divider on the outer `<header>`: **row 1** = route title (from the `pageTitles` map) + a trailing actions slot; **row 2** = a tab slot (`empty:hidden`, so tab-less pages stay a single title row). Pages portal chrome in via `page-header-actions.tsx`: `<PageHeaderActions>` (primary/secondary buttons — Import / Export / Add …, next to the title; gate with `GatedButton`'s `canAct` + `gateReason`, don't hide) and `<PageHeaderTabs>` (sub-navigation). Because the divider is on the whole header, a filled tab row makes the divider fall **after** the tabs, so the nav reads as part of the header. Tabs use `ui/tabs.tsx` **`variant="line"`** (underline style) driven controlled (`value`/`onValueChange`) — NOT a pill segment bar. Canonical example = `/leads` (actions) + `/members` (actions **and** line tabs, replacing its old `bg-muted` segment toggle). Every new page does the same.
- **Never hand-roll a UI element that exists in `src/components/ui/`.** Before writing any pill/button/input/dialog markup, check for a primitive there and use it. If no primitive fits, **stop and ask the user**: create a new master component, or use a different existing one? Don't silently roll an inline one-off.
- **Master components are single sources of truth.** A change to a file in `src/components/ui/` changes every usage across the product — **warn the user before editing one** and list what it affects. Restyling a single call-site goes through `className`/variants, never by forking the component.
- **No inconsistency in UI pattern tokens (non-negotiable).** Sibling components that read as the same *kind* of thing must share the same tokens — the popup padding, the border/ring, the radius, the item inset, the icon size, the muted-fill. Before you add or edit any component, open the **closest matching existing one** and copy its tokens verbatim; don't invent a value or eyeball it. Drift here is a real bug, not a nit: e.g. `DropdownMenuContent` puts `p-1` on the popup, but `SelectContent` had none — so a bare-item `ui/select` (no `SelectGroup`) rendered its items flush to the popup edge while a dropdown of the same shape looked padded (fixed by moving `p-1` onto the Select's `List`). When you notice such a mismatch anywhere, fix it at the master component so every call-site converges, and cross-check the peer components (menu ⇄ select ⇄ combobox ⇄ popover; the search field, the badge/pill family, the segmented toggle) for the same divergence.
- **Person avatars:** the canonical avatar is `UserAvatar` (`src/components/ui/user-avatar.tsx`) — photo when `src` (a `profiles.avatar_url` / future member photo URL) is set, first-initial fallback on the primary tint otherwise. **Every** person render (teammates in notes/rosters/sidebar, and future members/contacts in table or board views) goes through it so a photo uploaded once appears everywhere; never hand-roll `Avatar + AvatarImage + AvatarFallback` for a person again. Size via `size`/`className`, initial restyle via `fallbackClassName`, badge overlays (presence dots) as children. Teammate avatar URLs come from `useAccountStaff()` (`avatarById` map); the current user's from `useAuth().profile.avatar_url`.
- **Member identity cell:** the canonical way to render a person on the **Members page** is `MemberIdentity` (`src/components/members/member-identity.tsx`) — a `UserAvatar` next to the name over a communication line (**phone**; email is an equally valid comm string). Used in **every** member row/column so a person reads identically across the all-members table, renewals tables, follow-ups, trials, payment-due, inactive lists, check-in, and the payments ledger; never hand-roll a bare name+phone stack for a member again. Optional `meta` node = a third context line the caller styles (e.g. "plan · due date"); when `meta` is present the avatar top-aligns, else it centres on the name+phone pair. `src` = the member photo (`contacts.avatar_url`) — pass it at every call-site so an uploaded photo shows everywhere.
- **Member photo upload:** a member's photo lives on the pre-existing `contacts.avatar_url` (no migration) in the existing **`avatars`** public bucket (migration 008; path `{auth.uid()}/member-<contactId>-<ts>.webp`, RLS keys on the uid first segment, public read). Click the large avatar in the **member detail header** (`member-detail-view.tsx`, gated to `canSendMessages`) → `AvatarEditorDialog` (`avatar-editor-dialog.tsx`): view current, upload/change/remove, **square crop** via `react-easy-crop` (v6 — its structural CSS `react-easy-crop/react-easy-crop.css` must be imported, not auto-injected). On save the image is **optimized client-side** through `cropToWebp` (`src/lib/images/optimize.ts`) — square crop → cap at `MAX_AVATAR_PX` (512) → WebP at `AVATAR_WEBP_QUALITY` (0.82), so a multi-MB phone photo lands as a ~30 KB square. Writes chain `.select('id')` (silent-RLS-write rule) and best-effort GC the previous object. `onSaved` → the sheet's `refreshAll`, and because every render reads `contacts.avatar_url` via `MemberIdentity`/`UserAvatar`, the new photo appears product-wide at once.
- **Search fields:** the canonical search bar is `SearchInput` (`src/components/ui/search-input.tsx`) — a leading search glyph over a **rounded-rectangle** `Input` with the `border-border` token + muted fill. Width/flex go on the wrapper via `containerClassName` (e.g. `max-w-xs flex-1`); the radius/border/icon/padding are fixed — never restyle those per call-site (that drift — leads was a `rounded-full border-border` pill, members a `rounded-lg input-border` box — is exactly what this killed). Used by the leads + members + check-in toolbars, the inbox conversation list, and the import / manage-columns dialog pickers. The `Combobox`'s in-popover search and `global-search`'s command-trigger are deliberately their own patterns, not this.
- **Searchable selects:** the canonical type-ahead picker is `Combobox` (`src/components/ui/combobox.tsx`) — Select-styled trigger opening a Popover with a search input over **grouped** options plus an optional pinned footer action ("＋ Create…"). Use it for lists too long to scan (e.g. the import wizard's field picker); short static lists stay on `ui/select`. Don't hand-roll popover+input search UIs.
- **Data-grid column header:** the per-column header UI (label + single double-sided sort toggle + three-dot overflow menu carrying Sort ascending/descending, an Excel-style value **Filter** submenu, and column actions) is the shared `ColumnHeader` (`src/components/table/column-header.tsx`) — the single source of truth for both the `/leads` table and the All-members table. Generalized: freeze / add-column / edit-options / drag-handle / the greyed "smart property" placeholder are all **optional props**, so leads passes the full set and members mounts just Sort + Filter + Hide. Sort is ONE `ChevronsUpDown` button that cycles asc→desc (saves header width; shows the active direction lit). The resize grip + any drag transform live on the OWNING `<th>`, never in `ColumnHeader`. Leads' `HeaderCell` is now a thin adapter over it; members renders it directly. Don't fork it — restyle via props. The members table gained column **resize** + persisted **layout** (order/hidden/widths in the `members-all` `useTablePrefs` blob) + a toolbar "Columns" show/hide menu (the unhide surface); it deliberately still skips drag-reorder + freeze (low value on ~6 fixed columns). Column meta lives in `MEMBER_COLUMNS` (`members-table.tsx`), cell bodies in `renderCell()`, header Filter submenus wire to the shared `MemberFilters` dims (plan/status/fee) so header filter ⇄ Filters panel never drift.
- **Lead-field cells:** status pill / assignee chip / editor option lists live in `src/components/leads/lead-cell-renderers.tsx` (`StatusBadge`, `AssigneeDisplay`, `statusCellOptions` / `sourceCellOptions` / `genderCellOptions` / `assigneeCellOptions`, `customEditKind`) — consumed by BOTH the `/leads` table and the import preview grid so they can't drift. New surfaces rendering lead fields go through these.
- **Animation (Motion):** animate through the shared primitives in `src/components/ui/`, not scattered `motion.*` at call-sites. Canonical set: **`Collapse`** (`collapse.tsx`) — `open`-driven height+fade reveal that unmounts when closed (replaced the bulk toolbar's `grid-rows-[0fr↔1fr]` hack; the surrounding flex gap closes on its own); **`MotionList` + `MotionListItem`** (`motion-list.tsx`) — wrap a `.map` so items fade/slide in-out and FLIP-reflow on add/remove/reorder (`AnimatePresence mode="popLayout"`, `layout`; used on the notes list + `/notifications`); **`AnimatedNumber`** (`animated-number.tsx`) — count-up on scroll-into-view, drives text via ref (no per-frame re-render), honours reduced-motion (used in dashboard KPI tiles; pass `format` for currency). The **kanban board** (`leads-board.tsx`) uses `LayoutGroup` + per-card `layout="position"`/`layoutId` so a dragged card *flies* to its new column — dnd-kit owns the drag; its `DragOverlay dropAnimation` is `null` so Motion's FLIP owns the settle (don't re-enable it — double-animates). **The FLIP only stays smooth because every card is `contain: layout` (`[contain:layout]`)** — Motion flushes layout once per move to measure all cards, and containment makes each card an isolated layout subtree so that flush skips the card's ~30 internal nodes (avatar/SVG/badges/dropdown) and only re-lays-out the column's boxes. Without it, the rich card body froze the drop at scale (this bit us — the fix is containment, NOT stripping the animation). See the board-parity "Drag perf" note for the full render structure. **Gotchas:** (1) **Base UI dialogs/dropdowns/sheets/popovers already animate** via `data-open/closed` + `data-starting/ending-style` (tw-animate-css) — do NOT wrap them in Motion; it fights their mount lifecycle. (2) **Never put a `motion.*` (transform) on a `<tr>`/ancestor of the leads table** — a transformed ancestor becomes the containing block and **breaks `position: sticky` frozen columns**. Row enter/exit is intentionally not animated for this reason.
- **Badges/status pills:** the canonical pill is `Badge` (`src/components/ui/badge.tsx`). Fixed statuses use the tinted variants (`success` / `danger` / `warning` / `info` / `violet` — **fill-only** recipe `bg-{c}/10 text-{c}-400`, matching upstream shadcn `destructive`; no borders on pills). Admin-created **tags always render `variant="neutral"`** — the **slate** fill-only tint (`bg-slate-500/10 text-slate-500`), i.e. slate is treated as the neutral, non-colour-coded look and matches a `#64748b` `color`-prop status badge. DB-driven hex colours (lead statuses) use the `color` prop, which applies the same fill-only recipe inline. Domain wrappers (e.g. `MembershipStatusBadge`, `FeeStatusBadge` in `src/components/members/membership-status-badge.tsx`) map domain state → Badge variant; add wrappers like these rather than repeating variant choices at call-sites. Interactive chips (clickable tag toggles, removable filters) are buttons, not badges — don't force them into `Badge`.

## Roadmap (phased — build step by step, don't over-engineer)

- **Phase 1 — Core CRM foundation (the renewal wedge):** membership plans, member records, renewal action lists (expiring/expired/due), one-tap WhatsApp reminder, manual payment recording, basic staff assignment, simple owner tiles. **← Milestone 1, now built.**
- **Phase 2 — India-first workflows:** templated WhatsApp follow-ups, missed-lead reminders, trial tracking, auto expiry alerts (reuse `automations`), payment-due buckets, manual reconciliation, (later) UPI payment links/AutoPay.
- **Phase 3 — Retention & ops:** at-risk members, attendance/visit tracking, dormant recovery (reuse broadcasts), trainer accountability, owner reporting.
- **Phase 4 — Franchise / multi-branch:** `branches` + `branch_id` + RLS rework, branch dashboards, centralized member view, branch-scoped roles, standardized reports. Family/household plans slot in here.

**Deferred (don't build early):** branded member app, class marketplace, payroll, workout/nutrition tracking, franchise analytics, door access, AI automation, loyalty, UPI AutoPay.

### To-Do items

- **Meta lead-ads ingestion → `received_via='meta'`.** The Leads "Received By" column (migration `048`) already accepts a `'meta'` origin and renders "Auto · Meta", but **no code path creates a contact from a Meta lead ad** — the label is a reserved placeholder. When lead-ads capture is built (fits Phase 2 India-first / Phase 4 capture forms), set `received_via: 'meta'` on that insert and the column lights up automatically. Same reserved slot exists for `'automation'` (a future "create contact" automation step). See `src/lib/leads/attributes.ts` (`autoReceivedLabel`) and the create-path list in migration `048`'s header.

## Gym domain layer (data model)

A **member = a `contacts` row that also has a `memberships` row.** New tables (migration `031_gym_memberships.sql`):

- `membership_plans` — name, price, `duration_days`, `is_active` (soft-archive; a plan in use can't be hard-deleted, FK RESTRICT). Settings-class (admin write).
- `memberships` — one per member (`UNIQUE account_id, contact_id`): `plan_id`, `start_date`, `end_date` (expiry — the hot column), `status` (active/frozen/cancelled/**expired derived at read time**), `fee_amount`, `fee_status` (paid/due), `frozen_at`. Operational (agent write). Renewals mutate in place.
- `payments` — append-only ledger: amount, method (cash/upi/card/bank/other), `paid_at`, `period_start/end`, `screenshot_url/path` (chat-media). Delete = admin-only.
- `membership_periods` (migration `057`) — **one row per billing cycle = one invoice** (`period_start/end`, `fee_amount` snapshot, `state: open|void`, `UNIQUE(membership_id, period_end)`). The `memberships` row stays the **current-cycle pointer** (its start/end/fee mirror the live cycle, so every existing read keeps working); periods accumulate the HISTORY so recurring members get a real Paid/Unpaid/Upcoming trail + arrears. **View `membership_period_invoices`** adds `amount_paid`/`balance` by reconciling payments on `period_end` (the same trick as `membership_dues`, so **no `period_id` on payments**). Paid/Unpaid/Upcoming status is **derived in TS** (`periodStatus()` in `src/lib/memberships/periods.ts`) — it needs the account's tz "today", so it's kept OUT of SQL (view stays tz-agnostic). **Lifecycle:** an `AFTER INSERT` trigger on `memberships` (`create_initial_membership_period`) auto-creates the first period → all 5 create paths need **zero TS**. renew/edit/unfreeze/convert/cancel are explicit in TS (`periods.ts`: `insertRenewalPeriod` opens a new cycle, `syncCurrentPeriod` mirrors an edit, `setCurrentPeriodState` voids/reopens) — a trigger can't tell a renewal from an edit or an unfreeze. Renew opens a NEW period (old one stays = arrears); settling an old arrears invoice records against **that** period (RecordPaymentDialog gained an optional `period` prop) and does NOT touch the membership's live `fee_status`. Operational (agent write), realtime-published (054-style). The single *next* Upcoming invoice is **projected in TS** (`projectNextInvoice`, display-only — can't be real until it happens); past/current are persisted.

All date math is **IST-first** (`src/lib/memberships/expiry.ts`) — members must not expire a day early/late for a UTC+5:30 owner. "Expired" is derived (no cron needed for M1).

## Current status

**Milestone 1 (renewal wedge) is built and green**, and Phase 2/3 work is well underway. Members = a **top-level nav section** (`/members`) whose home is the Renewals action lists; Contacts stays the raw people+inbox table. The **Renewals tab** (`renewal-action-lists.tsx`) is two **vertically stacked tables** (Expiring in 7 days · Expired) borrowing the All-members table look; each row carries Assign / Renew / Remind actions. Payment-due lives on its own Payments tab (removed from Renewals). The **Expired table has a lookback filter** (Last 30 days / 3 months / 6 months / All time — client-filtered over the full expired set, default 30d = freshest chase targets). Plans managed at Settings → Membership plans. Key code: `src/app/(dashboard)/members/page.tsx`, `src/components/members/*`, `src/components/settings/plans-settings.tsx`, `src/lib/memberships/expiry.ts`, migration `031`.

Built since M1 (migrations `032`–`046`):

- **Leads module** (`/leads`, migrations `039`–`042`) — lead records on top of `contacts`, DB-driven lead status funnel (hex-coloured statuses), source/gender fields, **per-account editable lead field options**, custom fields with input types + formatting, INR currency handling, inline edit actions. Table = draggable/resizable/freezable columns (`page.tsx`), board = kanban by `lead_status`. **Sort:** real `contacts` columns sort server-side via `.order(sortColumn)`; everything else sorts **client-side** in `fetchContacts`' generalised `clientSort` branch (`ClientSort` = `custom | person | tags`) — pull all filtered lead ids, build a per-lead sort key, order the whole set (`compareCustomValues`: numeric types numerically, else lexical/`localeCompare` — imported dates are ISO so text order = chrono; blanks last), then fetch only the page's rows. Key source per kind: **custom** = the field's `contact_custom_values` value; **person** = the uuid column (`assigned_to`/`created_by`, on `ColumnDef.clientSort`) resolved to the teammate name via `nameById`; **tags** = each lead's alphabetically-first tag name (one account-scoped `contact_tags` read). A column is sortable if `sortColumn || isCustom || clientSort`.
- **Leads per-column value filter** (`page.tsx`) — every **filterable** column's header three-dot menu carries an Excel-style **Filter** submenu (`DropdownMenuSub` → plain `DropdownMenuItem`s with an always-visible left checkbox + `closeOnClick={false}`, so multi-select is obvious): a checkbox list of the column's possible values that show/hide rows. **Built-in** columns → shared `LeadFilters` dimension via `columnFilterConfig` (status→`leadStatus`, source→`source`, gender→`gender`, tags→`tags`, assignee→`assigned`, received_by→`owner`, created_by→`createdBy`), so the column filter and the global Filters panel are **one source of truth** (no drift). **Custom fields** of type text/number/currency (`CUSTOM_FILTER_TYPES`; email/phone/url/date excluded) filter too — distinct stored values are loaded into `customFilterOptions` (one account-scoped `contact_custom_values` read for visible filterable custom cols) and selections live in `LeadFilters.customValues` (`custom_field_id → values[]`). Free-text built-ins (name/phone/email/company/dates) omit the item. **Id-based filters** (tags + custom values) resolve to contact-id sets and **intersect** in `resolveContactIdFilter` (replaces the old tag-only `resolveTagContactIds` call; `applyLeadFilters`'s `idFilter` param does `.in('id', …)`) — used by the table, select-all, and CSV export. `LeadFilters.createdBy` filters `contacts.created_by` (also in the Filters panel). Header menu also: "Remove column" is now **Hide column** (`EyeOff` icon; still calls `hideColumn`).
- **Leads bulk actions** (`src/app/(dashboard)/leads/page.tsx` + `src/components/leads/bulk-*.tsx`) — row multi-select shows an **encapsulated toolbar below the search bar** that animates open/closed (grid-rows `0fr↔1fr` + fade, 300ms ease-in-out; the count is frozen mid-collapse so it can't flash "0", and `-mt-3` when closed cancels the flex gap). Actions:
  - **Edit** → `BulkEditDialog`: pick one property (status / assignee / source / gender / company / any custom field), set its value, apply to all selected. (Assign is folded in here as "Assigned to" — no separate Assign button.)
  - **Delete** → confirm + `.in('id', ids)`.
  - **Add note** → `BulkAddNoteDialog`, which **reuses the detail sheet's exported `NoteComposerCard`** (textarea + optional follow-up). Notes batch-insert (append-only); follow-ups insert per contact honouring one-OPEN-task-per-contact (skips are tallied, not errors).
  - **Convert to member** → `BulkConvertDialog`: pick plan + start date, create one `active` membership per lead (same insert as the detail-page convert) — converted leads drop off the list (leads anti-join memberships).
  - **Reuse pattern:** these dialogs' value pickers use `DropdownMenu` + `Badge`/`SourceIcon`/`UserAvatar` in the trigger (NOT `ui/Select` — its `SelectValue` echoes the raw value and its item padding differs), mirroring the inline table cell editors so status shows coloured pills, source/assignee show glyphs.
- **Leads CSV import 2.0** (`PRDs/import_leads_ux.md` — built Jul 2026). `ImportWizard` (`src/components/contacts/import-wizard.tsx`) is now **variant-parameterized**: `variant="contacts"` keeps the original 3-step flow; `variant="leads"` (wired on `/leads`) runs the 4-step flow — Upload → Map columns → **Preview & edit** → Confirm. Leads additions: lead-field mapping targets (`buildLeadTargets` in `field-mapping.ts`; raw cell text rides on `MappedRow`), searchable grouped field picker (`ui/combobox.tsx`), heuristic type detection on inline field-create + per-column `DD/MM` chip for ambiguous date columns (`detectFieldType` / `detectDateOrder`), an **editable preview grid** rendered with the leads table's own renderers (`import-preview-grid.tsx`, cap 200 rows shown, all imported), and the **Fix values panel** — value-level remapping with row counts, fuzzy auto-match, and a remap log that feeds the Confirm receipt + result audit. Coercion engine = `src/lib/leads/import-coerce.ts` (pure, tested): option/assignee matching, `buildPreviewRows`, `applyValueFix`. **Commit consumes the edited `PreviewRow[]`** (not a mapping re-run); insert payload extends with `lead_status`/`source`/`gender` and `assigned_to` (mapped assignee overrides importer-as-owner; updates never null ownership). Gotcha: unknown option values import as slugs (render as muted pills via `humaniseKey`) — safe by design.
- **Import → pending-teammate assignment** (migration `049`, built Jul 2026). The import Fix-values panel can assign leads to a teammate who **doesn't exist yet**: an admin picks "Invite '<name>' as a teammate", which find-or-creates an `account_invitations` row (role `agent`, `full_name` set) — reusing the invite system, NOT a parallel pending-staff table. Because `contacts.assigned_to` is FK→`auth.users(id)` and a pending invitee has no auth user, the parked assignment lives in **`contacts.pending_invitation_id`** (+ denormalized `pending_assignee_name` for display, so non-admin agents render it without reading the admin-only invites table); `assigned_to` stays the importer as the **fallback owner** (revoke/expire → lead degrades to importer, never ownerless). Leads render "Invite pending · name" via `PendingAssigneeDisplay`. Resolve a pending owner → real member **inline** (assignee cell), by **filter** ("Assigned to" lists pending invitees, values `pending:<id>`), or in **bulk** (BulkEdit "Assigned to") — all clear the overlay. On redeem, `redeem_invitation` (049) reassigns the parked leads to the joiner (assign-to-self → the notify trigger's self-guard suppresses the flood). Copy/rotate a shareable link from **Settings → Team → Pending invitations** (`POST /api/account/invitations/[id]/link` — tokens are hash-only, so each copy rotates and invalidates the prior link). Sentinel `PENDING_ASSIGNEE_PREFIX` in `import-coerce.ts`. **Scope:** pending owners are import-created only (not manually assignable from the normal picker) and don't count in round-robin/stats until they join.
- **Lead detail ↔ table sync** (`src/components/contacts/contact-detail-view.tsx`) — the detail sheet's **Details** section mirrors the leads table's columns in the same order, incl. custom fields (shared `custom_fields` fetch) and option lists (`useLeadFieldOptions`), so adding a column/custom field once surfaces it in **both**. Dropdown-editable rows = `InlineSelectField` (matches the table cell editors); read-only rows (Received by / Created) = `StaticField`. The notes thread (composer + note cards + follow-up-on-note + all its mutations) was **extracted to `src/components/contacts/contact-notes-thread.tsx`** (`ContactNotesThread`, keyed by `contactId`) so the member detail sheet mounts the identical surface; `NoteComposerCard` + `FollowUpDraft` / `DEFAULT_FOLLOW_UP_DRAFT` / `resolveDueDate` now live there and are **re-exported from `contact-detail-view.tsx`** so old import sites keep working.
- **Follow-ups** (migrations `036`, `043`–`045`) — assignable tasks with task types, due-date presets, IST reminder slots, and an optional link to the authoring note (one OPEN task per contact — cancel a note's open task before deleting the note). Notes live on the lead/contact detail sheet with author avatars, edit-in-place, drafts.
- **Payments & dues** — payment-due buckets (`034`), account UPI + copy-UPI-link (`038`), trial memberships (`035`), attendance (`032`), renewal reminder cron (`033`, `src/app/api/renewals/cron`), member activity (`037`).
- **Lead ownership transfer** (migration `050`, `PRDs/lead_ownership_transfer.md`) — role-gated. **Ownership = the "Received by" human = `contacts.user_id`** (NOT `assigned_to`, which stays a separate reassignable "assignment" field untouched by transfers). `received_via` remains the immutable origin CHANNEL; only **human-received** leads (`received_via` NULL/manual/import) are transferable — system-generated captures (whatsapp/meta/api/automation) are locked (RPC + UI both enforce). **Managerial (owner/admin):** transfer moves `user_id` instantly, new owner notified. **Peer handoff (agent):** transferring a lead they OWN opens an accept-gated request; `user_id` flips only when the target accepts — never ownerless (current owner holds; decline/cancel/supersede leave it). One entry RPC `request_lead_transfer` decides instant-vs-pending by role (`respond_lead_transfer`, `cancel_lead_transfer` complete the lifecycle); all three SECURITY DEFINER, `lead_transfers` SELECT-only from clients (writes via RPC). State machine `pending→accepted/declined/cancelled/superseded`; `uniq_lead_transfer_pending` = one pending per lead. Because ownership moves via `user_id` (not `assigned_to`), the `notify_lead_assigned` trigger doesn't fire — the RPCs notify the new owner explicitly (type `lead_assigned`) on admin-instant + admin-force-accept; a self-accept needs none. `notifications.reference_id` (new, generic) drives inline Accept/Decline on `/notifications`. **UI lives on the Received-by column** (leads table cell + detail sheet row): owner picker to start a transfer, `TransferPendingDisplay` overlay + Accept/Decline/Withdraw menu while pending; `TransferRequestDialog` for the agent note step; `lead_transfers` on realtime updates the overlay live. Predicates: `canReassignLeadsDirectly` (admin), `canRequestLeadTransfer` (agent+), `canResolveAnyLeadTransfer` (admin). Client lib `src/lib/leads/transfers.ts`.
- **Lead assignment approval** (migration `052`) — a SECOND flow on the **"Assigned to" column** (`contacts.assigned_to`, the delegate/worker, distinct from ownership). Owner (Received-by `user_id`) or admin change it **instantly**; **any other agent's change → a request the OWNER must approve** (approver = the owner OR any admin — NOT the target, unlike ownership transfer). Applies to any change incl. unassign. Reuses `lead_transfers` via a `kind` column (`'ownership' | 'assignment'`) + `approver_user_id`; `to_user_id` now nullable (unassign); one pending per `(contact_id, kind)`. RPCs `request_lead_assignment` (instant-vs-pending by owner/admin) / `respond_lead_assignment` (owner/admin approve/reject) / `cancel_lead_assignment`. Instant + approve paths write `assigned_to`, so the existing `notify_lead_assigned` trigger notifies the new assignee for free; the pending request notifies the **owner** (`lead_assignment_request`, 4 new notif types). UI: the Assignee cell routes through `requestLeadAssignment` (instant patch vs "sent for approval"), overlays a pending badge + Approve/Reject/Withdraw menu (`assignmentRequests` map, split from `fetchPendingTransfers` by kind); bulk assign loops the RPC per lead so agents can't bypass; `/notifications` gets inline Approve/Reject. Detail sheet Assigned-to row same. **Three distinct ownership facts** (don't conflate): `received_via` = origin channel (immutable, 048); **`created_by` = original human creator (immutable, migration `051` — set once at insert, frozen on update by trigger `lock_contact_created_by`; surfaced as the read-only "Created by" column + detail row)**; `user_id` = current owner ("Received by", transferable via 050). Deferred: account-wide pending-transfers console, auto-expiry cron.
- **Leads board parity** (Jul 2026) — the board (`leads-board.tsx`) now honours the shared **Filters panel**: `fetchBoard` runs `resolveContactIdFilter` + `applyLeadFilters` and is sequence-guarded like the table, and the Filters button renders in **both** views (Sort / Edit columns stay table-only — filters constrain the data, those are table presentation; without this a table-set filter kept applying to CSV export while invisible from the board). Drag-status writes set `updated_at` and chain `.select('id')` (empty = RLS-blocked → toast + revert refetch). Cards mirror the table row compressed: name + hover-reveal ⋮ menu (View/Edit/Delete — same page handlers as the table row menu; the card is a clickable **div**, not a `<button>`, because it contains real buttons), phone/company, 2 tag pills + "+n", footer = source glyph + compact created date vs the **owner slot** — assignee `UserAvatar`, or the amber pending chip for an in-flight ownership transfer / assignment approval / pending invite (same precedence as the table cells, so a lead mid-handoff can't look normal on the board; the `lead_transfers` realtime channel bumps `boardNonce` too). Board rows are tag-enriched (`BoardLead = Contact & {tags?}`; one account-scoped `contact_tags` read, no id list in the URL). Whole-board empty state matches the table's (filter-aware message + Add CTA). **Drag perf (load-bearing — the FLIP animation is kept; two traps neutralised):** (1) *During* drag — dnd-kit shares one React context, so every `useDraggable`/`useDroppable` consumer re-renders on each column crossing; the render structure keeps that cheap: `cardCtx` is ONE memoised object (stable ref, else the tree's memo is defeated); `ColumnCards` (memo, keyed on `leads`+`ctx`) is skipped when a column re-renders for its `isOver` outline, so the `motion.div` projection nodes it renders aren't touched mid-drag; `DraggableLeadCard` (the ONLY context subscriber) is a bare wrapper whose re-render doesn't reach the `motion.div` (its parent); `LeadCard` (memo body) is skipped. (2) *On* drop, two costs: (2a) Motion's FLIP does one layout flush to measure every card — **`contain: layout` on each `LeadCard` makes that flush skip all unchanged card internals** (count-dependent cost, collapses to a title+phone card); (2b) a fixed, **card-count-INDEPENDENT** cost — the optimistic status update must NOT be page state, or the drop frame re-renders the whole ~4k-line `LeadsPage` (toolbar, filters, ~10 always-mounted dialogs) and hitches the settle even at low card counts. So the board lives in an **island** `LeadsBoardView` (`leads-board-view.tsx`) that owns a LOCAL optimistic mirror of the fetched leads + the drag write; a drop re-renders only the island + board. The page stays fetch owner (`leads` prop drives the mirror, re-syncs via adjust-state-during-render on a new array identity — state guard `syncedProp`, not a ref, per the react-hooks lint); after the write commits the island calls `onStatusPersisted`, and the page syncs its board mirror + the table Status column inside `startTransition` so that low-priority re-render can't interrupt the in-flight FLIP. Cards use `layout="position"` (constant size) + `layoutId` (cross-column fly); the drop-container transitions only `background-color,outline-color` (never `transition-all`). If a huge board ever still drags heavy, the remaining lever is lowering `BOARD_LIMIT`. **Board settings (Tier 1)** — a gear now shows in board view too (fused right of the view picker, same split button; opens the shared `ViewSettingsSheet` switched on `view`). Two knobs, the board's peers of the table's page-size/cell-wrap: **card density** (`comfortable` shows company + tags + source/date footer; `compact` = name/phone/owner only) and **sort within column** (`newest`/`oldest`/`name`/`updated`, replacing the hard-coded newest-first; `sortColumnLeads` in `leads-board.tsx`, reorder animates via the FLIP). Both persist in the SAME `table_preferences` 'leads' blob under a `board:{density,sortWithin}` sub-object (no new migration; `useTablePrefs` shallow-merges the default in). The board island bumps `updated_at` optimistically so the `updated` sort reflects a drop instantly. **Board settings (Tier 2)** — `board.collapseEmpty` (Switch): hides 0-count status columns **at rest** but reveals every column **mid-drag** (`collapseEmpty && !activeLeadId`), so an empty stage stays a valid drop target; `handleDragEnd` still validates against `allColumns`, only `displayColumns` is filtered. Skipped hide-specific-status-columns (redundant with the Filters panel's status dimension). Deferred (Tier 3): **group-by** (pivot the board on source / assignee instead of status) — a real feature with a drag-semantics decision (dragging would set the grouped dimension: source-write vs the approval-gated `requestLeadAssignment`), not a lightweight pref; gender intentionally excluded as a group dimension.
- **Persisted table views** (migration `053`) — the leads table's column state (order / hidden / widths / `pageSize` / `viewMode` / `view` / `sort` / `frozenCount` / `board`, i.e. the `TablePrefs` blob — `board:{density,sortWithin}` added for the board Tier-1 settings) now persists **per-user, per-account** in `table_preferences` (`account_id`, `user_id`, `view_key`, `prefs jsonb`; UNIQUE on the three; RLS = own row only, `user_id = auth.uid() AND is_account_member`). Was a single global `localStorage` key (per-browser, account-agnostic, bled across accounts, no cross-device). New drop-in hook `useTablePrefs(viewKey, defaults)` (`src/hooks/use-table-prefs.ts`) keeps the `useLocalStorage` `[value, setValue]` API: paints from a per-scope `localStorage` cache on first frame, then the DB row wins on load; writes update state + cache synchronously and **debounce** the upsert (500ms, coalesces column-toggle bursts, flushed on unmount). `view_key='leads'` today plus `'members-all'` (All-members sort + page size + column order/hidden/widths); reuse the hook for `contacts` later. `useLocalStorage` remains for non-scoped prefs.
- **Members parity pass** (Jul 2026, migration `054`) — the Members module caught up with the leads-era infrastructure. **All-members table rebuilt** (`members-table.tsx`): server-paginated (`fetchSeq` guard, `.range()`, `contacts!inner` embed so search hits `contacts.name/phone` server-side; name sort via PostgREST embed-order `contact(name)`), toolbar **Sort** (reuses `LeadsSort` — it's generic) + **Filters** (`members-filters.tsx`, facets plan / derived status / fee_status) + **CSV export** of the filtered set; sort+pageSize persist via `useTablePrefs('members-all')`. Filter definition = pure `applyMemberFilters` (`src/lib/memberships/filters.ts`, tested) shared by table / select-all-matching / export — **"expired" is derived**, so its predicate is `status=active AND end_date < istToday()`, never `.eq('status','expired')`. **Bulk actions** (leads `Collapse` toolbar + frozen-count + select-all-matching): bulk WhatsApp remind (confirm dialog; `sendRenewalReminder` extracted from `send-reminder-button.tsx` for single+bulk reuse), bulk note/follow-up (reuses leads `BulkAddNoteDialog` — it gained a `noun` prop; selection is a `Map<membershipId, contactId>` because notes key by contact), bulk record-payment / mark-paid (`bulk-record-payment-dialog.tsx`, per-row inserts + `.select('id')` tallies). **Member detail gains the real notes thread** via the extracted `ContactNotesThread` (see the Lead-detail bullet; `onFollowUpChanged={refreshAll}` keeps the sheet's follow-up bar in sync); legacy one-line `memberships.notes` stays as-is. **Realtime**: `members/page.tsx` subscribes one `member-lists` channel on `memberships`/`payments`/`attendance` (published in `054`) → trailing-debounced `reloadKey` bump — all member tabs refresh live. **Members CSV import** = separate lightweight dialog `import-members-csv-dialog.tsx` (Upload → Map → Confirm), deliberately NOT a third `ImportWizard` variant — reuse is at the lib layer (`parseCsvRaw`, `normalizeKey`/`isUniqueViolation`, `detectDateOrder`) + new pure engine `src/lib/memberships/import-commit.ts` (tested: member column targets/auto-map, DMY-first `parseImportDate`, plan resolution, `buildMembershipRow` defaults start=today/end=start+duration/fee=plan price); commit = find-or-create contact (`received_via:'import'`) then **per-row** membership insert (`UNIQUE(account_id,contact_id)` → unique-violation = "already a member, skipped", batch inserts would die atomically). Fixes in the same pass: "View existing" dedupe link now resolves contact→membership (`src/lib/memberships/lookup.ts`) and opens the detail sheet; person renders route through `UserAvatar` (`avatarById`); plain `YYYY-MM-DD` dates render via `formatDay` (`src/lib/dates/format.ts` — parses date-only strings from parts, no TZ day-shift; the leads page's local `formatDate` was replaced by it). **Column machinery (added Jul 2026):** the All-members table now carries the leads-style per-column header — sort toggle + three-dot menu (sort/filter/hide) + resize + persisted layout — via the shared `ColumnHeader` (see the UI reusability → Data-grid column header bullet). Column meta = `MEMBER_COLUMNS`, cell bodies = `renderCell()`; a toolbar "Columns" menu unhides. Drag-reorder + freeze intentionally skipped (fixed ~6-column grid).
- **Member detail sheet 3.0** (Jul 2026, migration `056`) — the wide sheet (`data-[side=right]:sm:max-w-[960px]`) gained a **jump-nav + BMI rail + full profile/settings**. Structure: identity header (unchanged) over a `bg-muted/20` scroll body. A **sticky jump nav** (`ui/tabs.tsx` `variant="line"`, controlled by `activeSection`) scrolls to `#sec-<id>` sections and a scrollspy `IntersectionObserver` (`root`=scroll body, `rootMargin: "-56px 0px -60% 0px"`) lights the active tab; each `<Section>` carries `scroll-mt-14` so the sticky nav doesn't overlap. The **Membership** section's `⋯` menu carries lifecycle actions — Edit · Freeze/Resume · **Cancel/Reactivate** (`status='cancelled'`, destructive; swaps to Reactivate when cancelled). The **Payments** section is a **badged invoice list** backed by `membership_periods` (057): one clickable row per billing cycle (persisted past + current, newest-first) topped by the projected next (Upcoming) row, each with an `InvoiceStatusBadge` (Paid/Unpaid/Upcoming/Void — wrapper in `membership-status-badge.tsx`) computed by `periodStatus()`. Row click → **`InvoiceDetailDialog`** (a centered Dialog, NOT a nested drawer, since it opens over the right Sheet): that cycle's total/paid/balance + its reconciled payments, and a footer action — Record payment (settles that specific period via RecordPaymentDialog's `period` prop, so arrears hit the right cycle) or Renew (on the projected Upcoming row). The raw flat ledger stays below as "Payment history". Copy-UPI moved to the section header (shown when the current balance > 0). Trials don't bill (their 0-fee period is hidden). Data loads via a third `membership_period_invoices` fetch in the sheet loader. (This replaced the earlier single-`InvoiceLine` derived-row approach; the true recurring model is now built, not deferred.) Main column sections = **Membership · Payments · Notes** (`ContactNotesThread`) **· Attendance** (promoted from the old rail widget to a full section) **· Communication** (`member-communication.tsx` — STATIC dummy preview of the future WhatsApp send log, no data source yet) **· Personal info** (`member-personal-info.tsx` — editable form over the migration-056 contact columns, one Save; `name` stays a single field, gender reuses `GENDER_OPTIONS`) **· Settings** (`member-danger-zone.tsx` — Delete member only; Merge deferred). The **rail is BMI-only** (`bmi-card.tsx`, `lg:sticky lg:top-16`), replacing the old Follow-up + Visits widgets (follow-ups still live inside the Notes thread). **BMI:** pure lib `src/lib/bmi/bmi.ts` (`computeBmi` = `kg/m²`, WHO zones, `bmiGaugeFraction`, cm↔ft-in / kg↔lb — tested); hand-built SVG+CSS-transition gauge `bmi-gauge.tsx` (no gauge dependency; honours reduced motion). Height/weight store metric-canonical on `contacts.height_cm/weight_kg` (a future Vitals section reuses them); imperial accounts (`useLocale().locale.measurementSystem`) enter/read ft-in/lb, converted on save. Standard BMI ignores gender/age — neither is a BMI input. Missing measurements → an "Add measurements" empty-state (gated `canSendMessages`). **Delete** = the `delete_member(contact_id)` RPC (migration 056, SECURITY DEFINER) — re-checks owner/admin server-side (stricter than the agent-level `contacts_delete` RLS), purges the `payments` ledger (FK is SET NULL, not cascade) then deletes the contact (cascades membership/attendance/notes/follow-ups). UI gate = `canDeleteMember` (admin+, `roles.ts`). **New `contacts` columns (056):** `height_cm, weight_kg, date_of_birth, nickname, address_line1/2, city, state, postal_code, country` (all nullable; reuse existing `name/phone/email/gender`).
- **Billing periods / invoices** (Jul 2026, migration `057`) — recurring monthly/quarterly members now get a real per-cycle invoice trail (Paid/Unpaid/Upcoming) instead of a single mutated membership row. New `membership_periods` table + `membership_period_invoices` view + `src/lib/memberships/periods.ts`; the member-detail Payments section became a badged, clickable invoice list with an `InvoiceDetailDialog`. Full pattern (birth trigger, TS lifecycle, reconcile-by-`period_end`, TS-derived status, projected Upcoming) is in the **Gym domain layer** data-model bullet; UI is in the **Member detail sheet 3.0** bullet. Backfilled current + past-paid cycles from the ledger. Deferred still: auto-generating/charging future invoices (a billing cron — overlaps UPI AutoPay); persisting the Upcoming projection; per-cycle fee history for backfilled rows (fee = Σ paid).
- **Notes ownership** (`046`) — author-owned edit/delete + admin moderation (see the authored-content rule above).
- **Global `UserAvatar`** — see UI reusability section.
- **Motion animation layer** (`motion/react`, added Jul 2026) — reusable animation primitives + first call-sites. Kanban cards fly between columns (FLIP), the leads bulk-action toolbar collapses via `Collapse`, the notes list + `/notifications` animate add/remove/reflow via `MotionList`, and dashboard KPI tiles stagger-in + count-up (`AnimatedNumber`). Primitives + the "animate through `ui/` primitives" rule + the two hard gotchas (don't re-wrap Base UI overlays; no transforms over sticky frozen table columns) live in the **UI reusability → Animation (Motion)** section. Motion+ "AI Kit" (paid dev-tooling) was evaluated and **not adopted** — only the free MIT lib is in use.
- **Account-level localization** (Jul 2026, migration `055`) — the product now adapts to each gym's geography end-to-end; the full pattern (columns, `src/lib/locale/*`, `useLocale()`, presets-only geography, tz helpers) lives in the **Architecture → Localization** bullet. Shipped in the same change: signup country picker (preset → `handle_new_user` metadata), Settings → Localization section (registered in `settings-sections.ts`; country picker re-applies preset, live format preview; currency stays shared with Payments & currency — same column), renewal cron went **hourly** with a per-account 09:00-local send window + locale-formatted `{{3}}`/`{{4}}` template params (manual Remind button matches; `REMINDER_SEND_HOUR_LOCAL` in `renewal-reminders.ts`; workflow + `docs/renewal-reminders.md` updated), follow-up reminder slots resolve in the account tz (`remindAtInTz`/`slotFromRemindAt`), payment day-picks stamp via `dateAtNoonInTz` (noon-UTC anchor removed), check-in/summary "today" windows via `dayStartInTz`, `formatCurrency` gained a `locale` grouping param (en-IN → ₹1,00,000), `formatCustomFieldValue` gained `localeTag` (+ plain-date UTC-shift fix), `lib/dates/format.ts` (`formatDay`) **deleted** — every render goes through `fmt.date`. `loadGymStats(db, today, timeZone)`. `Account` type refreshed in `src/types/index.ts`. Deferred: dashboard chart internals (`lib/dashboard/date-utils.ts` still browser/server-local + Monday-first — cosmetic, charts only), `weekStart` stored+exposed but no calendar consumer yet (react-day-picker not in the app), phone default-region parsing (placeholders/hints are dynamic; bare local numbers still require an explicit country code — libphonenumber deferred), reminder-slot 12h labels fixed-English, WhatsApp template `language` still the template's own.

**One-tap reminder depends on** each account having WhatsApp connected + an **approved Meta Utility template `gym_renewal_reminder`** with 4 body params: `{{1}}` name, `{{2}}` plan, `{{3}}` expiry, `{{4}}` fee. Without it the Remind button self-disables with a setup hint; the rest works without WhatsApp.
